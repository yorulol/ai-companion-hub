{
  "name": "nova-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local AI agent service: OpenRouter (primary) + Ollama (backup), Discord bot, owner API for the web panels.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  }
}
